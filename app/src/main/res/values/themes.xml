<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.WeatherApp.Light" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!--Base colors-->
        <item name="colorPrimary">@android:color/holo_red_dark</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@android:color/holo_red_dark</item>
        <item name="android:windowBackground">@color/windowBackground</item>
        <item name="android:textColor">@color/textColorPrimary</item>

        <!--Component styles-->
        <item name="materialButtonStyle">@style/ButtonStyle</item>
        <item name="textInputStyle">@style/TextInputLayout</item>
        <item name="textColorDailyScreen">@color/black</item>
        <item name="materialCardViewStyle">@style/CardViewStyle</item>
    </style>

    <!-- Button style-->
    <style name="ButtonStyle" parent="Widget.MaterialComponents.Button.UnelevatedButton">
        <item name="android:backgroundTint">@android:color/transparent</item>
        <item name="iconTint">@android:color/black</item>
        <item name="iconSize">35dp</item>
    </style>

    <!--Style for TextInputLayout-->
    <style name="TextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.FilledBox">
        <item name="hintTextColor">@android:color/holo_red_dark</item>
        <item name="boxStrokeColor">@android:color/holo_red_dark</item>
    </style>

    <!--Style for CardView-->
    <style name="CardViewStyle" parent="Widget.MaterialComponents.CardView">
        <item name="cardBackgroundColor">@color/cardBackgroundColor</item>
    </style>

</resources>